# Deployment is now handled via GitHub Webhook → POST /webhook/github on the worker.
# See scripts/vps-deploy.sh for deploy logic.
name: "(webhook-based deploy)"
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy is triggered via GitHub webhook, not Actions."
