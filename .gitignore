node_modules/
.next/
dist/
coverage/
.env
.env.local
.env.bak.*
.vercel/
tmp/
logs/
data/runtime-cache/
secrets/
wildfire-540-*.json

# Scratch / experiment files at repo root — not source.
tmp_test.txt
_test_simple.txt
test-ps.txt
tmp-test.txt
pipe-test.txt
sample-heredoc.txt
trybash.txt

# Deploy tarballs — regenerated by build scripts, never source.
*.tgz

# One-off remediation scripts kept locally.
scripts/fix-*.sh

# Generated artifacts — regenerated on every build/run, never source.
# Tracking them caused a `git checkout`/`pull` conflict on every VPS
# deploy (prebuild rewrites the icons, the weather pipeline rewrites
# weather-data.json), aborting the deploy.
apps/web/public/favicon-16x16.png
apps/web/public/favicon-32x32.png
apps/web/public/apple-touch-icon.png
apps/web/public/icon-192.png
apps/web/public/icon-512.png
data/mock/weather-data.json
data/state/
